# libraries used in this project
pandas  # first install in terminal --> pip install pandas then import in notebook as --> import pandas as pd
psycopg2 # first install in terminal --> pip install psycopg2 then import in notebook as --> import pandas as psycopg2
sqlalchemy  #pip install sqlalchemy then --> from sqlalchemy import create_engine